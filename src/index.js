const React = require('react');
import ReactDOM from 'react-dom/client';

const { StoryGPTMain } = require('app');

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(React.createElement(StoryGPTMain));
